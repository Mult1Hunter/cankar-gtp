# registry/works/ - human-owned works registries

One `<author-slug>.jsonl` per author (WorkRecord schema); `NOTES.md` holds
analyst annotations. Hand-editable; tooling never clobbers `notes` fields.
Every corpus document must map to an entry here (ADR 0004).
