# configs/ - committed run definitions

One subdir per stage (mirrors cankar/). One file = one reproducible run
(ADR 0003: configs are files, never CLI flags). Currently: corpus/authors.toml
- the PD-author roster with death-year gates.
