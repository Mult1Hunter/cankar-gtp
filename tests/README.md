# tests/

Mirrors cankar/ per stage; `structure/` holds the layout law (root allowlist,
stage tuple, banned names - the repo's entropy surface); `fixtures/<stage>/`
holds golden + calibration fixtures (verbatim source bytes, hook-excluded).
